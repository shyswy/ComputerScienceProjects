import org.junit.jupiter.api.Test;

import java.io.File;

import static org.junit.jupiter.api.Assertions.*;

class FindVisitorTest2 {
/*
    private final int MAX= 10;

    @Test
    public void find_test() { //간단한 reverse의 decrpy 테스트
        DirectoryNode node = new DirectoryNode(new File("..")); //해당 경로 가진 파일 디렉토리에 추가
        node.accept(new FindVisitor("소공"));  //해당 keyword를 가진 file, dir만 가진 객체
        char[] arr=new char[MAX];
        char[] act=new char[MAX];
        char tmp;
        // EncryptWriter encryptWriter = null;
        try {
            for(int i=0;i<100;i++) {
                for(int j=0;j<MAX;j++) {
                    tmp = (char) ((Math.random() * 26) + 97);
                    arr[j]=tmp;
                }



                }



    }

 */
}